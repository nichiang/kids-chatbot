# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Optional: OpenAI Model Configuration
OPENAI_MODEL=gpt-4o-mini

# Optional: API Base URL (if using different provider)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Feature Flags
# Enable consolidated prompts for reduced latency (single API call vs multiple calls)
# Set to 'true' to enable, 'false' or omit to use individual API calls (backward compatible)
USE_CONSOLIDATED_PROMPTS=false